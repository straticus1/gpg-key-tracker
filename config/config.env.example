# GPG Key Tracker Configuration

# GPG home directory (default: ~/.gnupg)
GPG_HOME=/home/user/.gnupg

# Database path (default: ./gpg_tracker.db)
DATABASE_PATH=./gpg_tracker.db

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Custom GPG binary path
# GPG_BINARY=/usr/bin/gpg2

# Email Configuration (for report delivery)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# FROM_EMAIL=gpg-tracker@yourcompany.com

# AWS Configuration (for S3 uploads)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1

# SSH Configuration (for SCP uploads)
# SSH_USERNAME=your-username
# SSH_PASSWORD=your-password
# SSH_KEY_FILE=/path/to/private/key
